<h4><%- messages() %></h4>
<br>

<% if (errors) { %>
    <ul class="notice">
        <% errors.array().forEach(error => { %>
            <% console.log(error.msg); %>
            <% if(error.msg != "Invalid value") { %>
                <li><%= error.msg %></li>
            <% } %>
        <%  }) %>
   </ul>
<% } %>

<% if (title) { %>
    <h2><%= title %></h2>
<% } else {res.redirect('/')} %>
<form action="/account/signup" method="post">
    <fieldset>
        <label class="top">  First Name* <input type="text" id="account_firstname" name="account_firstname" required value="<%= locals.account_firstname %>"></label>
        <label class="top">  Last Name* <input type="text" id="account_lastname" name="account_lastname" required value="<%= locals.account_lastname %>" ></label>
        <label class="top">  Email* <input type="email" id="account_ëmail" name="account_email" required value="<%= locals.account_email %>" required pattern="[a-zA-Z0-9._%+-]+@byui\.edu$" title="Email"></label>
        <label class="top">  Password* <input type="password" id="account_password" name="account_password" required minlength="12" pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[^a-zA-Z0-9])(?!.*\s).{12,}$" title="Password must be at least 12 characters long, alphanumeric, include 1 capital letter, 1 special character"></label>
        <br><br>
    </fieldset>
    <br>
    <input type="submit" value="Create Account" class="submitBtn">
</form>